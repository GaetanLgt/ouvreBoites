{% extends "base.twig" %}

{% block head %}
	<title>{{ post.title }}</title>
{% endblock %}

{% block content %}
	<div class="rectangle8">
		<section>

			<div class="container mod-container pt-4 pb-5">
				<h1 class="contenuh1">{{ post.title }}</h1>
				<div class="contenu split">
					{{ post.content }}
				</div>

			</section>
		</div>
	{% endblock %}
